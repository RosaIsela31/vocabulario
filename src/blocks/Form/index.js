import React from "react";
import { Button, Input } from "../../components";

const Form = () => {
  return (
    <form className="w-50 p-3">
      <Input />
      <Button />
    </form>
  );
};

export default Form;
